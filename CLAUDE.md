# КПТ-дневник — контекст проекта для Claude Code

Этот файл лежит в корне репозитория `olgasamui/dnevnik-kpt`. Claude Code читает его автоматически в начале каждой сессии. После заметных изменений — обновлять разделы «Уже сделано» и «Следующие шаги».

> ⚠️ **ГЛАВНОЕ: вся работа идёт только в Дневнике 2 (папка `/v2/`, база во Франкфурте).**
> **Дневник 1 (корневой `index.html`, база в Сиднее) НЕ ТРОГАТЬ — ни код, ни базу.** Им пользуются живые люди. Описание Дневника 1 ниже — только для понимания, откуда всё взялось.

**Первое сообщение в новой сессии Claude Code:**
> Прочитай CLAUDE.md и посмотри код в папке /v2/ (Дневник 2 — с ним и работаем). Корневой index.html (Дневник 1) не трогай. Кратко перескажи, как ты понял проект и какой сейчас следующий шаг. Ничего не меняй, пока я не скажу.

## Как со мной работать

- Меня зовут Ольга, я не программист. Объясняй простым языком, по шагам, по-русски.
- У дневника есть живые пользователи. Главное правило: **ничего не должно сломаться, и ни одна запись не должна потеряться.**
- Любую SQL-команду сначала покажи и объясни, что она делает. **Перед каждым запросом к базе — по-русски: что делает и меняет ли что-то.** Необратимые действия (удаление, изменение данных) — только после моего «да».
- Claude подключён к GitHub и Supabase. В Supabase у Claude доступ только к базе (таблицы, SQL); настройки почты, шаблонов писем и адресов ссылок (Authentication) я меняю сама — Claude даёт пошаговую инструкцию.
- Не храни в коде пароли и секретные ключи (service_role / secret key — никогда). Publishable key в коде — это нормально.

---

## Дневник 1 — рабочая версия (ЗАМОРОЖЕН, только для справки)

- Сайт: https://kpt-dnevnik.com (домен на Namecheap, автопродление). Старый адрес olgasamui.github.io/dnevnik-kpt тоже работает.
- Хостинг: GitHub Pages, ветка `main`, файл `index.html` в корне (HTML + CSS + JS в одном файле).
- База: Supabase, проект `dnevnik-kpt`, регион **Sydney**, URL `https://igoxhbcjisounalughpi.supabase.co`. Publishable key — в `index.html`.
- Таблицы: `entries` (записи), `notes` (комментарии психолога; колонка `section` — комментарий к конкретному разделу).
- RLS включён: каждый видит только свои записи. Политика «Admin reads all entries» даёт olgabond1986@gmail.com читать все записи (для бэкапа).
- Вход по email без подтверждения почты. HTTPS включён. В Supabase Site URL = https://kpt-dnevnik.com.
- Психолог смотрит записи клиента по ссылке `?share=КОД` без логина и оставляет комментарии.

### Структура таблиц (Дневник 1)
- `entries`: `id` uuid, `user_id` → auth.users, `created_at`, `situation`, `emotions` jsonb (`[{name, cat, intensity}]`), `thoughts`, `distortions` jsonb, `body_reaction`, `needs` jsonb, `needs_text`, `constructive`, `author`, `share_code`
- `notes`: `id`, `entry_id` → entries, `text`, `color` (yellow/blue/green/pink), `section` (null = общий комментарий; иначе situation / thoughts / body / needs / constructive / author), `created_at`

### Откуда взялся дневник
Сделан по рабочей таблице с моих сессий у психолога. Колонки таблицы = шаги дневника:
1. Ситуация — безоценочное описание, как видеокамера
2. Эмоции и их накал 1–10
3. Мысли, схема, убеждения — умение увидеть когнитивные искажения
4. Реакции в теле и действия (или бездействие)
5. Потребности (физиологические, эмоциональные, социальные) — о чём позаботиться в себе, прежде чем влиять на других
6. Конструктивные мысли — как устроена реальность, какой я, какой другой человек, что нужно сделать, чтобы удовлетворить потребность (сказать, сделать, попросить, поменять способ)
7. Поведение автора жизни — который любит себя и экологичен к другим

### Что уже работает
- 7 шагов записи: ситуация → эмоции (накал 1–10) → мысли и искажения → тело и действия → потребности → конструктивные мысли → поведение автора жизни
- Можно дописать свою эмоцию и свою потребность
- Искажение «Проекция» с подсказкой
- Подсказки «Зона ответственности» (шаги 6–7) и вопросы от психолога в шаге 7
- Лента записей (новые сверху, группировка по датам), превью 3 строки
- Редактирование каждого раздела отдельно (✏️)
- Комментарии психолога к каждому разделу (💬 ответить) и общий комментарий
- Кнопка «📥 Скачать записи» — видна только админу, скачивает записи всех пользователей (без email, только анонимный user_id)
- Бэкап: раз в неделю через 📥 → Google Диск + iCloud/Dropbox

### Известная проблема (в Дневнике 1 пока не чиним)
- «Забыли пароль?»: после перехода по ссылке из письма новый пароль запрашивается через `prompt()`, а в Safari это окно не появляется. В Дневнике 2 сразу делать правильно — форма ввода нового пароля на странице (событие `PASSWORD_RECOVERY` в `onAuthStateChange`), без `prompt()`.

---

## Дневник 2 — новая версия для психологов (в работе)

- Сайт: https://kpt-dnevnik.com/v2/
- Код в папке `/v2/` этого же репозитория (один файл `v2/index.html`).
- Отдельная база Supabase: проект `dnevnik-kpt-v2`, id `osespyfocfvdslygjhfl`, регион **Frankfurt**. Полностью отдельно от Дневника 1 — **никогда не путать две базы**.
- Один сайт для всех (multitenant), единый тип аккаунта с полем `psychologist_id`.
- Дневник 1 не трогаем, пока Дневник 2 не протестирован полностью.

### Уже сделано и выложено
- Схема базы, кабинет психолога, коды клиентов (кабинет заменяет старую ссылку `?share=КОД`)
- Переключатель у каждой записи «видно психологу»
- Логика пробного периода и grace-периода
- Сброс пароля полностью на странице: «Забыли пароль?» → письмо → ссылка → форма «Новый пароль» (два поля). Устаревшая ссылка — понятное сообщение. Ошибки на русском. Проверено с компьютера.
- Почта: свой SMTP через Gmail `dnevnikkpt@gmail.com` (пароль приложения Google), отправитель «КПТ дневник». Письма приходят обычным пользователям.
- Supabase (Франкфурт): Site URL = `https://kpt-dnevnik.com/v2/`, Redirect URLs = `https://kpt-dnevnik.com/v2/**`. Шаблон письма «Reset password» — на русском.
- Вкладка «Дневник» (таблица `free_entries`, права и RLS уже были в базе): свободные записи — текст и дата, автосохранение, поиск, приватно по умолчанию, переключатель «видно психологу». В «Записях» переключатель «КПТ | Дневник». «+ Новая запись» спрашивает: КПТ или дневник.
- Кабинет психолога: дата тарифа с годом; оплата больше чем на 5 лет вперёд показывается как «Бессрочный доступ» (у меня paid_until = 2036).

### Блок «Вы психолог?» (связаться со мной)
Текст:
> Вы психолог? Ведите записи всех своих клиентов в одном кабинете. Зарегистрируйтесь на сайте и напишите администратору, который откроет вам доступ к личному кабинету психолога.

Контакты в блоке + кнопка «Написать в Телеграм»:
- Telegram: @olga_samui (без номера телефона)
- WhatsApp: мой номер (уже стоит в коде /v2/ — проверить)
- Email: `dnevnikkpt@gmail.com` (почта проекта создана). Домен собираюсь перенести с Namecheap — почту к домену не привязываем.

Сделано 25.09.2026. Где показывается:
- экран входа — ссылка «Вы психолог? Кабинет для специалиста →» открывает окно с блоком;
- «Настройки» у обычных пользователей (не психологов) — раздел «Для специалистов»;
- вкладка «Клиенты» у психолога — когда тариф бесплатный, закончился или пробный период на исходе: «Продлить или расширить доступ — напишите администратору» (вместо экрана оплаты).
Контакты заданы в коде константами `ADMIN_TG` и `ADMIN_MAIL`. WhatsApp пока не добавлен — нужен номер.

### Как добавляются психологи
- Психолог сам регистрируется → пишет мне → я выдаю кабинет и код через SQL. Вручную специально — чтобы был личный контакт.
- Пробный период 30 дней (до 10 клиентов) → предупреждение за 5 дней → 5 дней grace → доступ сужается до одного клиента (психолог выбирает какого; остальные записи сохраняются и возвращаются после оплаты).
- Продление считается от даты оплаты. Grace 5 дней — пока безусловный; пересмотрю, только если будут злоупотреблять.

### Бизнес-модель (на будущее)
- Тариф специалиста ~990 ₽/мес, клиенты бесплатно. Оплата пока вручную.
- Первый психолог — бесплатно навсегда за рекомендации; реферальная награда — месяц бесплатно обоим.
- НЕ превращать в полную платформу (без расписания, оплат сессий, заметок к сессиям). Только КПТ-дневник + обычный дневник.

### Еженедельная рутина в Supabase
- Сохранённые запросы: два бэкапа (entries, связи) в CSV с датой → в облако; три «кнопки» для психологов (сводка оплат, +1 месяц, новый психолог).
- Редкие и необратимые операции — в отдельном файле на компьютере, не в сохранённых запросах.

---

## Следующие шаги — всё в Дневнике 2 (по порядку)

### 1. Блок «Вы психолог?» — добавить WhatsApp, когда пришлю номер

### 2. Доделать почту и проверки
- Перевести на русский остальные шаблоны писем, если они используются (например, «Confirm sign up» — если включено подтверждение email).
- Проверить сброс пароля с iPhone (Safari).
- Когда пользователей станет много — перейти с Gmail на сервис рассылки (Supabase предупреждает, что Gmail — для личной почты).

**Важно (письмо от Supabase):** с 30 октября 2026 новые таблицы в схеме `public` не получают доступ к Data API автоматически. Для каждой новой таблицы явно выдавать права и включать RLS:

```sql
grant select, insert, update, delete on public.<таблица> to authenticated;
alter table public.<таблица> enable row level security;
```

До 30 октября 2026 проверить Security Advisor в обоих проектах Supabase.

### 3. Перевод пользователей с Дневника 1 на Дневник 2 (потом)
Только после полного тестирования Дневника 2. Продумать перенос записей без потерь.

---

## Правила безопасности
- Не менять и не удалять данные в таблицах без моего подтверждения.
- Корневой `index.html` (Дневник 1) и базу в Сиднее не менять вообще. Все изменения — только в `/v2/` и в базе во Франкфурте.
- Работать в отдельной ветке; в `main` — только после проверки (из `main` сайт публикуется сразу).
